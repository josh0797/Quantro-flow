# Quantro Flow frontend — placeholders only. Copy to .env.local (gitignored).
# Only REACT_APP_* values are embedded in the browser bundle — never put server secrets here.

REACT_APP_BACKEND_URL=http://localhost:8000
REACT_APP_SUPABASE_URL=https://YOUR_PROJECT.supabase.co
REACT_APP_SUPABASE_ANON_KEY=
